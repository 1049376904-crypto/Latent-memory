# Latent · 显影 部署报告

**日期**：2026-08-22
**fork**：fork 于 2026-08-19
**目标形态**：Kelivo (iOS) + 自有 VPS，远程 MCP
**当前状态**：服务端全绿，客户端连接未通

> 本报告中服务器 IP 与域名已替换为占位符：`<VPS_IP>`、`<SUB>.<DOMAIN>`。

---

## 一、环境

| 项 | 值 |
|---|---|
| 服务器 | 阿里云 杭州，Ubuntu |
| Python | 系统自带 `python3` |
| 客户端 | Kelivo，iOS |
| 出货档 | `--client generic` |
| 检索路线 | `zero-dep` |
| 语料 | Claude 官方导出 `conversations.json`，2.6 MB |
| 产出目录 | `/root/latent-output` |
| 隧道 | 复用已有 cloudflared 容器 |

补充：代码目录不是 clone 出来的，不是 git 仓库，因此无法提供 commit SHA，`git pull` 也不可用。

---

## 二、已跑通的部分

七步走全部通过，判据如下：

| 步骤 | 结果 |
|---|---|
| `inspect` | 语料读到，无现成人格文件，`blocking_issues: []` |
| `extract` | `candidate_count: 7`，`blocking_issues: []`，`warnings: []` |
| `choose-sections` | 十二节全部 `confirmed`，`unresolved: []` |
| `preview` | 全文正常，`preview_hash: cec6fc8a1b9c…`，`warnings: []` |
| `route` | `zero-dep` |
| `ship` | 四件套落盘 |
| 建库 | **未能通过 CLI 完成，见问题 1**；手动落盘后 23 个窗口文件 |
| `--doctor` | 103 块（叙事层 103），检索自查通过，2 项 ⚠ |
| 常驻 | systemd `latent-memory.service`，`active (running)` |
| 公网链路 | 宿主 `curl https://…/mcp` 带 token 回 **200** |

doctor 的两个 ⚠：无索引层（已知可选）、未配 `--timezone`（已在 systemd 补 `Asia/Shanghai`）。

---

## 问题 1（阻塞）：`--import` 被 argparse 收下但无任何代码消费

**现象**：`--step ship --import <导出文件>` 退出码 0、四件套照常打印，但 `memory/` 是空目录，连 `timeline/` 子目录都没被创建。

**定位**：

```
$ grep -n "import_path" src/memory_init.py
（零命中）
```

`write_bundle()` 的 docstring 写着「entries 给了就把语料落成记忆库（write_corpus）；没给就只把目录建出来」，实现是 `if entries:`。但 CLI 的 ship 分支从未把 `--import` 的值转成 entries 传进去。

**影响**：这正是项目文档反复警告的那类失效——**看起来完全像成功**。`--doctor` 是唯一能发现它的地方，而按《给AI的引导指南》的步骤顺序，doctor 在 ship 之后（第 8 步 vs 第 6 步）。跳过体检直接接客户端，会得到一个「连上了但什么都记不住」的空壳，排查方向会被引向客户端侧。

**试过的错误路径**：`--corpus` 也不触发落盘，它只影响体检和配置里的路径。

**绕过办法**（两个函数都是项目自己的，未另写解析逻辑）：

```bash
cd <repo>/src
python3 -c "
import memory_import, memory_init
entries = memory_import.load_any('<导出文件路径>')
written, backup = memory_init.write_corpus('<产出目录>/memory', entries)
print(len(entries), len(written))
"
# 输出：1709 23
```

之后 doctor 全绿。

---

## 问题 2（阻塞级可用性）：输入清单缺 `anchors` 锚表，schema 不接受 `source_anchor`

《给AI的引导指南》第 2 步明确写着：

> ⚠ **定位是挑锚号，不是自己数字符**：输入清单里每个来源都附了一张 `anchors` 锚表，起止已经算好，模型填 `source_anchor` 就行。

实际生成的 `人格候选输入清单.json` 全文：

```json
{
  "sources": [
    {
      "source_ref": "<导出文件路径>",
      "sha256": "f16de06791e486879bc12f8394468c8716804e43aed222f388cb612e2b05ba5e",
      "kind": "corpus"
    }
  ]
}
```

没有 `anchors` 字段。`人格候选结果.schema.json` 里也没有 `source_anchor`，`source_span` 是必填。

**影响**：模型只能自己算字符偏移，而这恰好是文档自己指出的高错误率环节（「实测两家模型 7 条候选里 6 条把区间圈错」）。

**叠加的第二层困难**：Claude 官方导出的 JSON 里中文是 `\uXXXX` 转义形态，而校验器按文件原始字符逐字比对（`memory_init.py` 1724–1727 行 `read_text()` 读成纯文本 blob）。也就是说：

- 文件总字符数 2,639,093，是 ASCII 空间的长度
- `raw.find('妍妍')` 返回 `-1`，`raw.find('\\u598d')` 返回 `197`
- 一个汉字在偏移空间里占 6 个字符

任何「读解码后的内容、再填 span」的做法都必然错位，且报出来的错是「evidence 不是原文逐字」，看不出根因。

**我们的解法**：写脚本用 `json.dumps(text, ensure_ascii=True)[1:-1]` 把锚点文本编码成文件里的形态，再 `find` 定位、原样切片，span 与 evidence 全部由脚本现算。一次过闸，`blocking_issues: []`。

**建议**：要么补上文档描述的 anchors 锚表，要么在文档里说明中文导出的转义问题并给出编码方法。

---

## 问题 3（文档与代码不一致）：`--index-dir` 不存在

README 与《给AI的引导指南》多处给出这条命令：

```
python src/mcp_server.py --doctor --corpus <...> --index-dir <产出目录>/memory/index --threads <...>
```

实际：

```
mcp_server.py: error: unrecognized arguments: --index-dir <产出目录>/memory/index
```

`mcp_server.py --help` 的参数表里没有 `--index-dir`。README 中「由 `mcp-config.json` 的 `--index-dir` 合并读取」这句描述也对不上。

---

## 问题 4（误导性提示）：`memory/` 为空时 doctor 建议指向 `persona-extraction`

```
✗ 语料文件：<产出目录>/memory 下（含子目录）没找到任何 .md 语料。
  同级的这些目录里有 md，你要指的多半是其中之一：persona-extraction。
```

`persona-extraction/` 里是提取任务的提示文档（`人格候选提取提示.md`），不是语料。照这个建议改 `--corpus` 会把提示文档当语料建库。真正的原因是建库没跑（问题 1），不是路径指错。

**建议**：目录为空（而非「指到了别处」）时，把提示指向「ship 有没有真的建库」，而不是推荐同级目录。

---

## 问题 5（健壮性）：token 含非 ASCII 时崩连接，而非返回 401

服务端日志：

```
HTTP 请求处理异常（<内网IP>）：TypeError: comparing strings with non-ASCII characters is not supported
```

这是 `hmac.compare_digest` 的行为。结果是连接被 reset，cloudflared 侧看到 `EOF`，公网回 **502**。

**未能确定非 ASCII 来自哪一侧**：服务端配置里那串实测 `isascii() == True`，因此怀疑是客户端发来的 header 值，但没能在 Kelivo 侧确认。

**建议**：比较前显式检查，回 401 并说明「token 含非 ASCII 字符」。当前形态会把 token 格式问题伪装成「服务挂了」，而 502 会把排查引向网络层——实际排查中确实走了这条弯路。

---

## 问题 6（版本一致性）：实际工具名无 `latent_` 前缀

`--doctor` 输出：

```
✓ MCP 接线：握手 2025-06-18，工具 5 个：
  memory_search、session_start、memory_append、memory_correct、thread_close
```

README 描述的是 `latent_search` / `latent_session_start` / `latent_append` / `latent_correct` / `latent_thread_close`，并专门说明前缀是为避免与宿主自带同名工具撞车，还记录了一例外部实测：

> 一位 Kelivo 用户在无前缀的旧版上连不上，把工具名自定义之后就能正常用了。

出货的 `persona.md` 里写的是无前缀名，与实际工具名自洽，内部没有矛盾。但同一份 fork 里 README 描述与代码行为不一致，不确定是 fork 时间点问题还是上游本身状态。

**这一条可能与下面的故障相关**——README 那例实测正是「Kelivo + 无前缀 = 连不上」。

---

## 三、当前未解决的故障

**现象**：Kelivo (iOS) 连 `https://<SUB>.<DOMAIN>/mcp`

- 手机开代理：**能连通**
- 手机不开代理：`Connection reset by peer (OS Error, errno = 54)`

**关键对照**：同一台服务器、同一个 cloudflared 容器、同一条隧道上另外两个 hostname，在不开代理条件下长期稳定服务中：

- `<SUB_A>.<DOMAIN>/mcp` — 另一套 MCP 服务，正在使用
- `<SUB_B>.<DOMAIN>/v1` — API 网关，正在使用

三个 hostname 的 DNS 形态完全一致（Cloudflare 橙云代理，返回同一对 anycast IP）。

### 已排除项

| 假设 | 排除依据 |
|---|---|
| 服务未运行 | systemd `active (running)`；`ss -tlnp` 显示 python3 蹲在 `172.17.0.1:8765` |
| 语料未入库 | doctor 103 块，拿语料自己的标题回查命中 |
| token 错误 | 宿主带 token 打 `127.0.0.1:8765` 回 200；公网 `https://…/mcp` 带 token 也回 200 |
| DNS 记录差异 | 三个 hostname 形态一致 |
| cloudflared 出站代理 | 容器的 `https_proxy` 只作用于出站连边缘，与入站方向无关 |
| 容器到宿主不可达 | 服务从 `127.0.0.1` 改绑 `172.17.0.1` 后宿主 curl 回 401/200，且 cloudflared 曾成功代理过（当天 12:26 公网回 200） |

### 待验证的假设（按优先级）

1. **Kelivo 的 `Accept` 头与响应形态不匹配**
   README 说服务按 `Accept` 决定给单事件 SSE 还是 JSON，并注明「这两条只有自检盖着、没有真机」。Kelivo 若发的是只认 SSE 的形状，可能触发未经真机验证的路径。

2. **工具名无前缀导致撞车**
   见问题 6。README 记录的那例 Kelivo 实测与当前现象吻合。

3. **该 hostname 的特定可达性**
   同隧道另两个 hostname 正常服务中，此项优先级最低。

### 下一步计划（未执行）

在已验证可达的 hostname 上加一条 `path: ^/mcp` 的 ingress 规则指向 `172.17.0.1:8765`，排除掉可达性变量，从而把范围收到客户端侧。

---

## 四、给开发者的优先级建议

按影响面排序：

1. **问题 1**（`--import` 无消费）——静默失效，且失效形态与成功几乎无法区分
2. **问题 2**（缺 anchors + 中文转义）——冷启动用户在 extract 这一步会卡住，且报错信息指不到根因
3. **问题 5**（token 非 ASCII 崩连接）——把配置问题伪装成网络问题，实际排查中造成弯路
4. **问题 3、4、6**——文档与代码一致性
